# Reconstruct minimal design attributes from a data frame

Reconstruct minimal design attributes from a data frame

## Usage

``` r
reconstruct_design_attributes(data)
```

## Arguments

- data:

  A data.frame with valid CBC design structure

## Value

The data with reconstructed attributes
