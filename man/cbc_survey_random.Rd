% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{cbc_survey_random}
\alias{cbc_survey_random}
\title{Create a random survey directly from profiles}
\usage{
cbc_survey_random(
  profiles,
  n_resp,
  n_alts,
  n_q,
  no_choice = FALSE,
  label = NULL
)
}
\arguments{
\item{profiles}{A \code{cbc_profiles} object created by \code{cbc_profiles()}.}

\item{n_resp}{Number of survey respondents}

\item{n_alts}{Number of alternatives per choice question}

\item{n_q}{Number of questions per respondent}

\item{no_choice}{Include a "no choice" option in the choice sets? Defaults to \code{FALSE}.}

\item{label}{The name of the variable to use in a "labeled" design (also
called an "alternative-specific" design) such that each set of alternatives
contains one of each of the levels in the \code{label} attribute. Defaults to \code{NULL}.}
}
\value{
A data frame with class \code{cbc_survey} containing the complete random survey design
}
\description{
This function creates a random survey by sampling directly from profiles,
bypassing the structured design step. Each respondent sees a unique random
set of choice questions.
}
\examples{
# Create profiles
profiles <- cbc_profiles(
  price = c(1, 2, 3),
  type = c("A", "B", "C")
)

# Create random survey directly from profiles
survey_random <- cbc_survey_random(
  profiles = profiles,
  n_resp = 100,
  n_alts = 2,
  n_q = 4
)
}
