% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{compare_designs}
\alias{compare_designs}
\title{Compare balance and overlap across multiple designs}
\usage{
compare_designs(...)
}
\arguments{
\item{...}{Multiple \code{cbc_design} objects to compare, or named arguments where
each argument is a \code{cbc_design} object}
}
\value{
A data frame comparing balance and overlap metrics across designs
}
\description{
Compare balance and overlap across multiple designs
}
\examples{
# Create profiles
profiles <- cbc_profiles(
  price = c(1, 2, 3),
  type = c("A", "B", "C")
)

# Create different designs
design_random <- cbc_design(profiles, n_alts = 2, n_q = 4, method = "random")

priors <- cbc_priors(
  profiles = profiles,
  price = -0.5,
  type = c(0.2, 0.3)
)

design_sequential <- cbc_design(
  profiles, priors = priors, n_alts = 2, n_q = 4, method = "sequential"
)

# Compare designs
compare_designs(
  Random = design_random,
  Sequential = design_sequential
)
}
